## Node Scaling Experiment

This project contains the resources used to test how NodeJS scales with CPU.
The app is available through docker hub at `harrymartland/node-load-app`.
If you would like to reproduce the load test run the AWS CDK script and then run the artillery scripts against it.
